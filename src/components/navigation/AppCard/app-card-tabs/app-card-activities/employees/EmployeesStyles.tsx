import { style } from "typestyle";

const EmployeesStyles = {
  container: style({
    width: 800,
    height: 400,
  }),

  text: style({
    marginTop: 10,
    textAlign: "center",
    fontSize: 14,
    fontWeight: "bold",
    color: "#008080",
  }),
};

export default EmployeesStyles;
