import { style } from "typestyle";

const AppTabStyle = {
  container: style({
    width: 100,
    height: 40,
  }),

  textName: style({
    fontSize: 15,
    fontWeight: "bold",
  }),
};

export default AppTabStyle;
